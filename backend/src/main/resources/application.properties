spring.application.name=trupload-api
server.port=8081

# Keep request logging useful during local development without exposing secrets.
logging.level.com.trupload=INFO

# PostgreSQL connection. Set DB_* environment variables for your local or deployed database.
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:trupload_dev_db}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DB_USERNAME:postgres}
# spring.datasource.password=${DB_PASSWORD:Lalli@srkr1}
spring.datasource.password=${DB_PASSWORD:Polign@123}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

app.jwt.secret=TRUploadJwtKey2026A7f9K2mP8xQ4vN6zR1
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:3600000}
